# AgentForge Environment
# Copy to .env and fill in values. NEVER commit .env.

# Required: Anthropic API key for Claude Code
ANTHROPIC_API_KEY=

# Optional: Default output directory (defaults to output/)
# AGENTFORGE_OUTPUT_DIR=

# Optional: Logging level (debug|info|warn|error, defaults to info)
# LOG_LEVEL=info
